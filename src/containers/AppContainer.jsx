import React from "react";
import "../assets/styles/components/AppContainer.styl";
const AppContainer = ({ children }) => (
  <div className="appContainer">{children}</div>
);
a;
export default AppContainer;
